import { Box, Typography } from '@mui/material';
import React from 'react';

export default function ChangePasswordPage(): React.ReactElement {
  return (
    <Box>
      <Typography variant="h1" sx={{ paddingTop: 200, margin: 'auto', width: '50%' }}>
        Change Password ...
      </Typography>
    </Box>
  );
}
